package com.Projectile;

import com.Constants.GeoConstants;

public class Calculation {

	public int get_range(float initial_velocity, float launch_degree) {
		launch_degree = launch_degree * Math.PI / 180
		float range = (initial_velocity*initial_velocity)*Math.sin((2*launch_degree));
		return 0;
	}

}
